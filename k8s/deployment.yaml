apiVersion: apps/v1
kind: Deployment
metadata:
  name: "newtest"
  namespace: "agents-support"
  labels:
    app: "newtest"
    team: "support"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "newtest"
  template:
    metadata:
      labels:
        app: "newtest"
        team: "support"
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: "sa-newtest"
      containers:
        - name: "newtest"
          image: "acrshoppoc.azurecr.io/agents/newtest:latest"
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: "config-newtest"
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 5
